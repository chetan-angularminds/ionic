import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Online_Movie_ticket_booking_app',
  webDir: 'www'
};

export default config;
